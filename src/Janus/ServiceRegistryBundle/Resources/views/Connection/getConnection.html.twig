{% extends "JanusServiceRegistryBundle::layout.html.twig" %}

{% block title "Symfony - Connection" %}

{% block content_header '' %}

{% block content %}
    <h1 class="title">Connection #{{ id }}</h1>

    <p>{{ connection }}</p>
    <p>
        <a href="{{ path('get_connections') }}">{{ 'connection.list.link'|trans({}, 'JanusServiceRegistryBundle') }}</a> <br>
        [<a href="{{ path('remove_connections', {'id': id}) }}">{{ 'connection.remove.link'|trans({}, 'JanusServiceRegistryBundle') }}</a>]
        [<a href="{{ path('edit_connections', {'id': id}) }}">{{ 'connection.edit.link'|trans({}, 'JanusServiceRegistryBundle') }}</a>]
    </p>
{% endblock %}